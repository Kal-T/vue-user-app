package com.devtracker.userapp.config;

/**
 * TODO: add security later.
 * Currently, Spring Security dependencies are omitted from the build path in pom.xml,
 * meaning all incoming traffic and local CORS pre-flight calls are permitted by default.
 */
public class SecurityConfig {
}
